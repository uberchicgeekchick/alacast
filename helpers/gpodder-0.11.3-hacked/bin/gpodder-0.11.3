#!/bin/tcsh
set gPodders_path = `dirname ${0}`

if ( ! -x "${gPodders_path}/gpodder" ) then
	printf "Unable to find and run `%s/gpodder`" $gPodders_path
	exit -1
endif

set starting_point = `pwd`

cd "${gPodders_path}"
./gpodder --run --local "${argv}"

cd "${starting_point}"

